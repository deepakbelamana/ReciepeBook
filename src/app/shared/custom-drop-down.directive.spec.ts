import { CustomDropDownDirective } from './custom-drop-down.directive';

describe('CustomDropDownDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomDropDownDirective();
    expect(directive).toBeTruthy();
  });
});
